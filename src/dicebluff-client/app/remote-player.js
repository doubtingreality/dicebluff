'use strict';

function RemotePlayer() {
    //
}

RemotePlayer.prototype.rollDice = function(diceCount, eyesCallback) {
    //
};

RemotePlayer.prototype.evaluateClaim = function(
    totalDiceCount,
    claimEyesList,
    handEyesList,
    wildcardPermitted,
    verdictCallback
) {
    //
};

RemotePlayer.prototype.reviseClaim = function(
    totalDiceCount,
    claimEyesList,
    handEyesList,
    wildcardPermitted,
    claimCallback
) {
    //
};
